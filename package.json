{
  "name": "openapi-test",
  "version": "0.5.0",
  "description": "API to test adding OpenAPI to an existing App",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "PORT=3000 node server.js",
    "dev": "NODE_ENV=test PORT=3005 nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "swagger-ui-express": "^4.1.2",
    "yamljs": "^0.3.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.3"
  }
}
